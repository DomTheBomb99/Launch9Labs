'use client'
import {useEffect,useRef} from 'react'
export default function Experience(){const ref=useRef<HTMLCanvasElement>(null);useEffect(()=>{const c=ref.current;if(!c)return;const x=c.getContext('2d');if(!x)return;let raf=0,mx=0,my=0,t=0;const pts=Array.from({length:75},()=>({x:Math.random(),y:Math.random(),s:.3+Math.random()*1.5,v:.15+Math.random()*.5}));const resize=()=>{c.width=innerWidth*devicePixelRatio;c.height=innerHeight*devicePixelRatio;x.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0)};const move=(e:MouseEvent)=>{mx=e.clientX;my=e.clientY};const draw=()=>{t+=.006;const w=innerWidth,h=innerHeight;x.clearRect(0,0,w,h);const g=x.createRadialGradient(mx,my,0,mx,my,360);g.addColorStop(0,'rgba(216,255,69,.13)');g.addColorStop(1,'transparent');x.fillStyle=g;x.fillRect(0,0,w,h);pts.forEach((p,i)=>{p.y-=p.v/10000;if(p.y<-.03)p.y=1.03;const px=p.x*w+Math.sin(t+p.y*8)*20,py=p.y*h;x.beginPath();x.arc(px,py,p.s,0,Math.PI*2);x.fillStyle='rgba(255,255,255,.32)';x.fill();if(i%5===0){x.beginPath();x.moveTo(px,py);x.lineTo(px+(mx-px)*.035,py+(my-py)*.035);x.strokeStyle='rgba(155,124,255,.08)';x.stroke()}});raf=requestAnimationFrame(draw)};resize();addEventListener('resize',resize);addEventListener('mousemove',move);draw();return()=>{cancelAnimationFrame(raf);removeEventListener('resize',resize);removeEventListener('mousemove',move)}},[]);return <canvas ref={ref} className="experienceCanvas" aria-hidden="true"/>}
