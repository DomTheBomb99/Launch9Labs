import { createClient } from '@/lib/supabase/server'
import { Reveal, Magnetic } from '@/components/motion'
export const revalidate=0
export default async function Contact(){const supabase=await createClient();const{data:s}=await supabase.from('site_settings').select('*').limit(1).maybeSingle();const email=s?.contact_email||'hello@launch9labs.com';return <main className="shell page"><nav className="nav"><a className="brand" href="/">{s?.site_name||'LAUNCH9 LABS'}</a><div className="navLinks"><a href="/projects">Projects</a><a href="/products">Products</a><a href="/updates">Updates</a><a href="/about">About</a></div><Magnetic><a className="navCta" href="/">Home <span>↗</span></a></Magnetic></nav><header className="pageHeader"><span className="sectionNumber">05 // CONTACT</span><h1>Let's build<br/><em>something useful.</em></h1></header><Reveal><section className="contactPanel"><div><span className="pulse"/> OPEN TO IDEAS</div><p>For product questions, partnerships, ideas, or just saying hello, reach out. We read everything.</p><Magnetic><a className="contactMail" href={`mailto:${email}`}>{email}<span>↗</span></a></Magnetic></section></Reveal><section className="contactFooter"><span>LAUNCH9 / 2026</span><a className="back" href="/">← Back home</a></section></main>}
