import { createClient } from '@/lib/supabase/server'
export const revalidate=0
export default async function Contact(){const supabase=await createClient();const{data:s}=await supabase.from('site_settings').select('*').limit(1).maybeSingle();const email=s?.contact_email||'hello@launch9labs.com';return <main className="shell page"><a className="brand" href="/">{s?.site_name||'LAUNCH9 LABS'}</a><header className="pageHeader"><span className="sectionNumber">05 // CONTACT</span><h1>Let's build<br/><em>something useful.</em></h1></header><div className="aboutCopy"><p>For product questions, partnerships, ideas, or just saying hello, reach out.</p><a className="button primary" href={`mailto:${email}`}>{email} ↗</a></div><a className="back" href="/">← Back home</a></main>}
