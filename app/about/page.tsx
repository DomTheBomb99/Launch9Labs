import { createClient } from '@/lib/supabase/server'
export const revalidate=0
export default async function About(){const supabase=await createClient();const{data:s}=await supabase.from('site_settings').select('*').limit(1).maybeSingle();return <main className="shell page"><a className="brand" href="/">{s?.site_name||'LAUNCH9 LABS'}</a><header className="pageHeader"><span className="sectionNumber">04 // ABOUT</span><h1>An independent<br/><em>software studio.</em></h1></header><div className="aboutCopy"><p>{s?.description||'Launch9 Labs is a small software studio focused on building useful digital products, tools, and experiments.'}</p><p>We favor simple ideas, strong execution, and interfaces that feel considered rather than manufactured by a template.</p></div><a className="back" href="/">← Back home</a></main>}
